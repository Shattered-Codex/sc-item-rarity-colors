/**
 * Base strategy for building runtime rarity CSS blocks.
 */
export class BaseRuntimeStyleStrategy {
  /**
   * @param {object} _context
   * @returns {string[]}
   */
  buildRules(_context) {
    return [];
  }
}
